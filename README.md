ParallelADB
=============
Enable adb command parallel run in all connected devices.

Usage
-------
Use `parallel-connect.py {text file containing newline-separated IP addresses} [root]` to connect to devices in parallel. Parameter 'root' is optional.

Adapt and use `parallel-cmd.py` to run adb commands in parallel.
